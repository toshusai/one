{
  "name": "@toshusai/one",
  "version": "1.0.0-alpha.3",
  "description": "a js framework in an 1.7KB one class",
  "author": "toshusai",
  "license": "MIT",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "tsc": "tsc --project tsconfig.json --declaration",
    "build": "esbuild index.ts --bundle --outfile=index.js --minify",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.17.15",
    "typescript": "^5.0.3"
  }
}
